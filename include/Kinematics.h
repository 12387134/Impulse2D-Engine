#include "RigidBody.h"
#include "Vector2D.h"
#include <vector>

class World {
    // make a vector to hold all rigid bodies
    // automatically push back rigid bodies
    // for loop to handle collision detection
    // apply gravity
};